<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!--
      Tela cheia da oferta, sobre o que estiver no aparelho e sobre a tela de
      bloqueio. No Android 14+ ela nao e concedida automaticamente para
      aplicativos que nao sao de chamada ou despertador: sem a liberacao manual,
      o Android rebaixa a oferta para notificacao com som em vez de descartar.
    -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      <!--
        Chave do Google Maps. O valor vem do Gradle, que le
        `android/local.properties` — fora do Git, porque o repositorio e
        publico. Se vier vazia o aplicativo abre normalmente e o mapa fica
        cinza.
      -->
      <meta-data
        android:name="com.google.android.geo.API_KEY"
        android:value="${mapsApiKey}" />
      <!--
        Recebe o push de oferta e monta a notificacao de tela cheia. Precisa ser
        nativo: mensagem com bloco `notification` e desenhada pelo Android sem
        passar pelo aplicativo, e ai nao ha como pedir tela cheia.
      -->
      <!--
        Responde aos botoes de aceitar e recusar da notificacao. `exported`
        falso: so o proprio aplicativo dispara estas acoes, e deixar aberto
        permitiria que outro aplicativo respondesse ofertas em nome do motoboy.
      -->
      <receiver
        android:name=".OfferActionReceiver"
        android:exported="false">
        <intent-filter>
          <action android:name="com.motoboycity.driverapp.OFERTA_ACEITAR" />
          <action android:name="com.motoboycity.driverapp.OFERTA_RECUSAR" />
        </intent-filter>
      </receiver>
      <service
        android:name=".OfferMessagingService"
        android:exported="false">
        <intent-filter>
          <action android:name="com.google.firebase.MESSAGING_EVENT" />
        </intent-filter>
      </service>
      <!--
        O app precisa de UM unico FirebaseMessagingService. O servico proprio
        herda o comportamento do React Native Firebase (inclusive renovacao do
        token) e acrescenta a apresentacao nativa da oferta.
      -->
      <service
        android:name="io.invertase.firebase.messaging.ReactNativeFirebaseMessagingService"
        tools:node="remove" />
      <service
        android:name=".DeliveryLocationTrackingService"
        android:exported="false"
        android:foregroundServiceType="location" />
      <!--
        Activity translucida exclusiva da oferta. Ela nao aparece nos recentes
        e nao transforma a Activity principal inteira em uma janela autorizada
        a cobrir a tela bloqueada.
      -->
      <activity
        android:name=".OfferActivity"
        android:theme="@style/OfferTheme"
        android:launchMode="singleTop"
        android:excludeFromRecents="true"
        android:showWhenLocked="true"
        android:turnScreenOn="true"
        android:exported="false" />
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>
    </application>
</manifest>
